{
  "name": "jishi-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute jishi-db --file=./schema.sql",
    "db:migrate": "wrangler d1 migrations apply jishi-db"
  },
  "devDependencies": {
    "wrangler": "^4.14.0"
  }
}
